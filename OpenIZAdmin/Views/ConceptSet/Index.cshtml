@model IEnumerable<OpenIZAdmin.Models.ConceptSetModels.ConceptSetViewModel>
@{
    ViewBag.Title = Locale.ConceptSet;
}

<div id="search-partial">
    @Html.Partial("_SearchPartial", new OpenIZAdmin.Models.SearchModel())
</div>


@section scripts{
    <script>
        $(document).ready(function ()
        {
            $.ajax({
                url: "Search?searchTerm=*",
                type: "GET",
                beforeSend: function ()
                {
                    showLoadingBar();
                },
                success: function (data)
                {
                    $("#search-results").html(data);
                },
                complete: function ()
                {
                    hideLoadingBar();
                }
            });

            $('#search-partial').bind("DOMSubtreeModified", function ()
            {
                if (!$('#conceptSetSearchTable').length)
                    return;

                if ($.fn.DataTable.isDataTable('#conceptSetSearchTable'))
                    return;
                
                $('#conceptSetSearchTable').dataTable({
                    order: [],
                    columns: [
                      { "orderable": false },
                      null,
                      null,
                      null,
                      { "orderable": false }
                    ]
                });
            });
        });
    </script>
}