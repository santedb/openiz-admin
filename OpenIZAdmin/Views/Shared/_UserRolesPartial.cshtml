@model IEnumerable<OpenIZAdmin.Models.UserAdministration.ViewModels.UserRoleViewModel>
@{
    ViewBag.Title = Resources.UserRoles;
}

<div class="panel panel-default">
    <div class="panel panel-heading">
        @Resources.UserRoles
    </div>
    <div class="panel panel-body">
        @if (Model.Count() > 0)
        {

        }
        else
        {
            <p>@Resources.NoUserRolesAdded .</p>
        }
    </div>
</div>

