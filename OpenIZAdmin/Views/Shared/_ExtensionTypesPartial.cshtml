@model IEnumerable<OpenIZAdmin.Models.ExtensionTypeModels.ExtensionTypeViewModel>
@{
    ViewBag.Title = Locale.ExtensionTypes;
}

<div class="panel panel-default">
    <div class="panel-heading">        
        <span class="panel-title">@Locale.ExtensionTypes</span>        
    </div>
    <div class="panel panel-body">
        @if (Model.Any())
        {
            <table class="table table-striped table-bordered" id="extensionTypesTable">
                <thead>
                <tr>
                    <th></th>
                    <th>@Html.DisplayNameFor(m => m.Name)</th>
                    <th>@Html.DisplayNameFor(m => m.HandlerClass)</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="centerContent autoFitContents"><span class="glyphicon glyphicon-lock" aria-hidden="true"></span></td>
                            <td>@item.Name</td>
                            <td>@item.HandlerClass</td>
                            <td class="autoFitContents centerContent">
                                <a class="btn btn-primary" href="@Url.Action("ViewExtensionType", "ExtensionType", new {id = item.Id})">
                                    @Locale.View
                                    <span class="glyphicon glyphicon-eye-open"></span>
                                </a>
                            </td>
                        </tr>}

                </tbody>
            </table>
        }
        else
        {
            <p>@Locale.NoAdditionalDetails</p>
        }

    </div>
</div>

